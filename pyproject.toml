[project]
name = "pyutils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hachoir>=3.3.0",
    "pillow>=12.0.0",
    "pyyaml>=6.0.3",
    "win32-setctime>=1.2.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.0",
]
